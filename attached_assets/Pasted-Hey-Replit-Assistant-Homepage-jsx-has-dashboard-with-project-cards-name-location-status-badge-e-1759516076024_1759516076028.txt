Hey Replit Assistant! Homepage.jsx has dashboard with project cards (name, location, status badge, edit/delete). Redesign "Your Projects" to Google Wallet-style: Horizontal scrolling row at top (like bank cards, flex overflow-x-auto space-x-4), each card compact w-64 h-32 rounded-xl with color edging border-4 based on profitability (from stored project.profitMargin or Viability PoGDV%: green #22c55e for 22%+, amber #f59e0b for 16-21%, red #ef4444 for <15%). Add photo upload for custom image (tap card "Image" → file input, store base64 in project.image, show on card; default house icon). Tap card navs to phases (setSelectedProjectId, setCurrentPhase('site')). Fork to "dashboard-wallet".

1. Update src/components/Homepage.jsx dashboard:
 - Replace list with <div class="flex overflow-x-auto space-x-4 pb-4 snap-x snap-mandatory"> {projects.map(project => <div key={project.id} className={`card w-64 h-32 rounded-xl p-4 shadow-md border-4 cursor-pointer ${project.profitMargin >= 22 ? 'border-green-500' : project.profitMargin >= 16 ? 'border-amber-500' : 'border-red-500'}`} onClick={() => { setSelectedProjectId(project.id); setCurrentPhase('site'); }}><img src={project.image || '🏠'} class="w-full h-16 rounded object-cover mb-2" alt="Project"/><div class="text-primary font-medium">{project.name}</div><div class="text-muted text-sm">{project.location}</div><button onClick={(e) => { e.stopPropagation(); const input = document.createElement('input'); input.type = 'file'; input.accept = 'image/*'; input.onchange = (e) => { const file = e.target.files[0]; if (file) { const reader = new FileReader(); reader.onload = (ev) => updateProject(project.id, {image: ev.target.result}); reader.readAsDataURL(file); }; input.click(); }} class="btn-ghost text-xs absolute top-2 right-2">📷</button><button onClick={(e) => { e.stopPropagation(); handleDelete(project.id); }} class="btn-ghost text-xs text-danger absolute bottom-2 right-2">×</button></div>)} </div>.
 - Add "New Project" card at end: Similar style, neutral border-gray-500, onClick creates blank, sets ID, navs to 'site'.

2. Update utils/storage.js: updateProject to handle image base64.

3. Test: Cards scroll horizontal, tap card → phases for that project, border colors by profitMargin (assume from Viability). Tap 📷 → upload image, shows on card.

Output: Updated Homepage.jsx, storage.js. Dark theme, mobile-first!
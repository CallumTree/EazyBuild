Hey Replit Assistant! Homepage.jsx dashboard has wallet-style cards (horizontal scroll, profitMargin borders, image upload, delete). Current design dated (flat, boxy). Modernize to Visa card style: Rounded-xl (2xl radius), soft shadow-md, gradient border (green #22c55e to #10b981 for 22%+, amber #f59e0b to #d97706 for 16-21%, red #ef4444 to #dc2626 for <15%), glossy image overlay, crisp font (text-primary bold, text-muted italic). Keep scroll (snap-x), tap card navs to phases, tap 📷 uploads image (base64), X deletes. Fork to "dashboard-modern-v1".

1. Update src/components/Homepage.jsx dashboard:
 - Replace div with <div class="flex overflow-x-auto space-x-4 pb-4 snap-x snap-mandatory"> {projects.map(project => <div key={project.id} className={`card w-64 h-32 rounded-2xl p-4 shadow-md border-4 bg-gradient-to-br ${project.profitMargin >= 22 ? 'from-green-500 to-emerald-600' : project.profitMargin >= 16 ? 'from-amber-500 to-orange-600' : 'from-red-500 to-rose-600'} cursor-pointer relative overflow-hidden`} onClick={() => { setSelectedProjectId(project.id); setCurrentPhase('site'); }}><div class="absolute inset-0 bg-white/20 backdrop-blur-sm"></div><img src={project.image || '🏠'} class="w-full h-16 rounded-lg object-cover mb-2 z-10 relative" alt="Project"/><div class="text-primary font-bold z-10 relative">{project.name}</div><div class="text-muted text-sm italic z-10 relative">{project.location}</div><button onClick={(e) => { e.stopPropagation(); const input = document.createElement('input'); input.type = 'file'; input.accept = 'image/*'; input.onchange = (e) => { const file = e.target.files[0]; if (file) { const reader = new FileReader(); reader.onload = (ev) => updateProject(project.id, {image: ev.target.result}); reader.readAsDataURL(file); }; input.click(); }} class="btn-ghost text-xs z-10 relative top-2 right-2">📷</button><button onClick={(e) => { e.stopPropagation(); handleDelete(project.id); }} class="btn-ghost text-xs text-white z-10 relative bottom-2 right-2">×</button></div>)} </div>.
 - Add "New Project" card: Same style, border-gray-500 gradient, onClick creates/saves/navs to 'site'.

2. Update utils/storage.js: Ensure updateProject handles image base64.

3. Test: Cards scroll with snap, modern Visa vibe (rounded, gradient, glossy), tap → phases, tap 📷 uploads image, shows sleek.

Output: Updated Homepage.jsx, storage.js. Dark theme, mobile-first!